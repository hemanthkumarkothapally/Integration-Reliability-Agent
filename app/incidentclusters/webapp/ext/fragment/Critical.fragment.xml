<core:FragmentDefinition
  xmlns:core="sap.ui.core"
  xmlns:macros="sap.fe.macros">

  <macros:KPITag
    number="{kpi>/criticalCount}"
    unit="Critical"
    status="{kpi>/criticalStatus}"
    showIcon="true" />

</core:FragmentDefinition>